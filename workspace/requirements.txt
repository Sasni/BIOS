# BIOS Analysis Toolkit - Requirements

# Core
flask>=2.0

# Optional: SecureBoot X.509 certificate parsing
# Without this, secureboot_audit.py will still work but won't decode
# certificate subjects, issuers, validity, or key details.
cryptography>=41.0

# For UEFI parsing (optional, more advanced)
# uefi-firmware-parser>=1.0

# For entropy analysis (optional)
# scipy>=1.10  # for proper entropy calculation